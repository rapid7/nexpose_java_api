<EngineSaveRequest 
   session-id="${session-id}"
   sync-id="${sync-id}">
   <EngineConfig
      id="${engineConfigId}"
      name="${engineConfigName}"
      address="${engineConfigAddress}"
      port="${engineConfigPort}"
      priority="${engineConfigPriority}">
      ${sitesGenerator}
   </EngineConfig>
</EngineSaveRequest>